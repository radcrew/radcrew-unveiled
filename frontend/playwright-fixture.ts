// Shared Playwright test/expect. Re-export the base from @playwright/test so
// specs import from one place; extend here later if custom fixtures are needed.
export { test, expect } from "@playwright/test";
